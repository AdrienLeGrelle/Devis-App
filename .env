# Single dev port: React dev server must use 3000 (enforced by scripts/check-port-3000.js before npm run dev)
PORT=3000
# Do not open browser when starting React; Electron will load http://localhost:3000
BROWSER=none
